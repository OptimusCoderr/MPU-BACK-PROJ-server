{
  "name": "spineguard-api",
  "version": "1.0.0",
  "description": "SpineGuard — Backend API (deploy separately on Vercel)",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev"   : "vercel dev",
    "deploy": "vercel --prod",
    "seed"  : "node lib/seed.js"
  },
  "dependencies": {
    "dotenv"   : "16.4.5",
    "mongoose" : "8.3.4",
    "pusher"   : "5.2.0"
  }
}
